<p id="notice"><%= notice %></p>

<h1>Mazes</h1>

<table>
  <thead>
    <tr>
      <th>Adjacencylist</th>
      <th>Solutionmaze</th>
      <th>Startingpoint</th>
      <th>Endpoint</th>
      <th>Sizemaze</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mazes.each do |maze| %>
      <tr>
        <td><%= maze.adjacencyList %></td>
        <td><%= maze.solutionMaze %></td>
        <td><%= maze.startingPoint %></td>
        <td><%= maze.endPoint %></td>
        <td><%= maze.sizeMaze %></td>
        <td><%= link_to 'Show', maze %></td>
        <td><%= link_to 'Edit', edit_maze_path(maze) %></td>
        <td><%= link_to 'Destroy', maze, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Maze', new_maze_path %>
